---
kind: Service
apiVersion: v1
metadata:
  name: openhands-mcp-git-server
  labels:
    app: openhands-mcp-git-server
spec:
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
  selector:
    app: openhands-mcp-git-server
